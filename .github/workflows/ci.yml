name: Rust

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    container: aosc/aosc-os
    steps:
    - name: Install Dependency
      run: |
        sudo apt update
        sudo apt-get install -y rustc opencc
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose
